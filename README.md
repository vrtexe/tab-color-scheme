# tab-color-scheme

Go to [about:config](about:config) in firefox.

Search for: `svg.context-properties.content.enabled`

And set it to `true`.

This change is required for the icon to show correctly based on the theme.
